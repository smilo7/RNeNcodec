# rnencodec/configs/quickstart_train.yaml
dataset_root: "artifacts/data/waterfill_quickstart_hf_dataset"  # after extraction
batch_size: 8
epochs: 1
learning_rate: 0.0005
num_workers: 2
save_dir: "artifacts/checkpoints"
save_every: 1
model:
  codebooks: 4
  hidden: 512
  layers: 2
  # add other hyperparams as needed for GRUModelConfig
